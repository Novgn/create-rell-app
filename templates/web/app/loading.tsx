// Next.js App Router loading UI. Rendered while the route's async boundary is
// resolving. Skeleton components replace this placeholder in Story 4.3.
export default function Loading() {
  return (
    <main>
      <p>Loading…</p>
    </main>
  );
}
