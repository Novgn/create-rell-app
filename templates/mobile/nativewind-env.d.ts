/// <reference types="nativewind/types" />

// NativeWind 4 augments React Native component prop types to allow a
// `className` prop — this reference directive is what makes TypeScript
// understand `<View className="flex-1 bg-white" />` without an error.
